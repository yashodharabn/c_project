name: Hello world
on:
  push:
    branches:
      - main
jobs:
  furst-hello-wirkd-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Print message
        run: echo " hello world of github"
